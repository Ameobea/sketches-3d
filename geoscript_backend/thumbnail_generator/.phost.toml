[app]
name = "geoscript-thumbnail-renderer"

[build]
commands = ["just docker-build"]
image = "geoscript-thumbnail-renderer:latest"

[deploy.run]
net = "host"
# Reaps the Chrome processes puppeteer leaves behind; without it hundreds of zombies pile up.
extra_args = ["--init"]
